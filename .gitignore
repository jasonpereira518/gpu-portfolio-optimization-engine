.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/

# Cached market data — regenerate with `make data`, never commit
data/cache/
*.parquet

# Benchmark outputs are regenerated by `make bench`; the committed results/
# CSVs referenced in the README are added explicitly with `git add -f`.
benchmarks/results/*.csv
benchmarks/results/*.png
benchmarks/results/environment.json

.DS_Store
